# CLAUDE.md

<!-- Seeded by loop-tool `init` because this repo had no CLAUDE.md.
     Claude Code (and every loop iteration) auto-loads this file, so it is
     where the repo tells the agent how it actually works. Fill it in and
     delete these comments. Keep it TIGHT — it loads every iteration;
     commands + conventions + explicit "does NOT do", not prose. -->

## How this repo is tested

<!-- The real test command(s), the narrowest way to run one file/test,
     and the framework. If the repo's CI runs the tests, name the CI
     command here — that is the reproducible truth of "green". -->

- Test command:
- Run a single test:
- Framework / where tests live:

## How this repo is built / run

<!-- Build command, dev-server boot, any required services (DB, docker). -->

## Conventions the agent must follow

<!-- Patterns to conform to, not override: error handling, module layout,
     naming, how new code is wired in. -->

## What this repo deliberately does NOT do

<!-- The most important section for loop-tool. Explicit absences stop the
     agent inventing. Examples:
     - "Module X has no unit tests by design — do not add one; if a change
        needs coverage, raise it, don't reshape the code to test it."
     - "No platform branching in shipped code; test setup lives in CI." -->
